# Client Bootstrap subtheme starter

This theme is a minimal Drupal Bootstrap subtheme scaffold.

## Usage

1. Enable the base theme and subtheme:
   - `drush then bootstrap client_bootstrap -y`
2. Set as default:
   - `drush config:set system.theme default client_bootstrap -y`
3. Extend templates, CSS, and libraries per-project.
